name: Update badges

on:
  schedule:
    # Runs at 2am UTC
    - cron: "0 2 * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: clunkiersalt817/badge-readme@main
        CREDLY_USER : Kshitiz Sharma
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
